# Runtime dependencies for the HealthIntel FastAPI backend.
fastapi>=0.115,<1.0
pydantic>=2.7,<3.0
pypdf>=5.0,<7.0
python-multipart>=0.0.9,<1.0
uvicorn[standard]>=0.30,<1.0

# Install this backend package so `healthintel_api` is importable.
-e .
